rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 로그인 전 초기 사용자 생성용
    match /users/{userId} {
      allow read, write: if true;
    }
    // 그 외 컬렉션: 로그인한 사용자만 접근
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
